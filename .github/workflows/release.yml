# Release workflow: packages repo and creates a tag based on version number when pushed to main

name: Release

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  VERSION: "0.5.7"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Git user
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Package repository
        run: |
          zip -r OuterParallax-MPE_${{ env.VERSION }}.zip . -x '*.git*' -x '*.github*' -x 'GameData/OuterParallax/OPM/*'
          zip -r OuterParallax-OPM_${{ env.VERSION }}.zip . -x '*.git*' -x '*.github*' -x 'GameData/OuterParallax/MPE/*' -x 'OuterParallax-MPE_${{ env.VERSION }}.zip'

      - name: Create and push tag
        run: |
          git tag ${{ env.VERSION }}
          git push origin ${{ env.VERSION }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION }}
          name: Release ${{ env.VERSION }}
          files: |
            OuterParallax-MPE_${{ env.VERSION }}.zip
            OuterParallax-OPM_${{ env.VERSION }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  upload-MPE:
    needs: release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download release zip
        run: |
          wget https://github.com/${{ github.repository }}/releases/download/${{ env.VERSION }}/OuterParallax-MPE_${{ env.VERSION }}.zip

      - name: Login to Spacedock
        run: |
          curl -F username=jthero3 -F password=${{ secrets.SPACEDOCK }} -c ./cookies "https://spacedock.info/api/login"

      - name: Update Mod
        run: |
          curl -b ./cookies \
          -F "version=${{ env.VERSION }}" \
          -F "changelog=Quick Fix" \
          -F "game-version=1.12.5" \
          -F "notify-followers=no" \
          -F "zipball=@OuterParallax-MPE_${{ env.VERSION }}.zip" \
          "https://spacedock.info/api/mod/3834/update"

  upload-OPM:
    needs: release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download release zip
        run: |
          wget https://github.com/${{ github.repository }}/releases/download/${{ env.VERSION }}/OuterParallax-OPM_${{ env.VERSION }}.zip

      - name: Login to Spacedock
        run: |
          curl -F username=jthero3 -F password=${{ secrets.SPACEDOCK }} -c ./cookies "https://spacedock.info/api/login"

      - name: Update Mod
        run: |
          curl -b ./cookies \
          -F "version=${{ env.VERSION }}" \
          -F "changelog=Quick Fix" \
          -F "game-version=1.12.5" \
          -F "notify-followers=no" \
          -F "zipball=@OuterParallax-OPM_${{ env.VERSION }}.zip" \
          "https://spacedock.info/api/mod/3833/update"