// Changelog file generated by yaclog-ksp (https://git.offworldcolonies.nexus/drewcassidy/yaclog-ksp)
KERBALCHANGELOG
{
    modName = OuterParallaxContinued
    VERSION {
        version = 0.7.1
        versionDate = 2026-08-04
        CHANGE {
            type = Changed
            change = Release packaging now runs through KSPBuildTools, producing separate MPE and OPM archives.
        }

        CHANGE {
            type = Changed
            change = Version numbers are now generated from a `.versiontemplate` at release time.
        }

    }

    VERSION {
        version = 0.7.0
        CHANGE {
            type = Changed
            change = Released before this changelog was kept; see the GitHub release notes.
        }

    }

    VERSION {
        version = 0.5.5
        CHANGE {
            type = Changed
            change = Updated scaled space atmosphere textures.
        }

    }

    VERSION {
        version = 0.5.3
        CHANGE {
            type = Added
            change = Compatibility patch for OPX.
        }

        CHANGE {
            type = Fixed
            change = Fixed Ervo's icebergs.
        }

    }

    VERSION {
        version = 0.5.2
        CHANGE {
            type = Added
            change = Compatibility patch for Sigma Dimensions rescales.
        }

    }

    VERSION {
        version = 0.5.1
        CHANGE {
            type = Changed
            change = Updated all MPE textures for MPE v1.1.1.
        }

    }

    VERSION {
        version = 0.4.0
        CHANGE {
            type = Changed
            change = Final pass, for now, on scatters across all OPM and MPE bodies.
        }

        CHANGE {
            type = Changed
            change = Final pass, for now, on all scaled space bodies.
        }

        CHANGE {
            type = Fixed
            change = Fixed Soden.
        }

        CHANGE {
            type = Known Issues
            change = Edas scaled space still shows significant terracing; the original MPE textures may need to be redone.
        }

    }

}
